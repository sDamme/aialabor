import { InfoOverlayDirective } from './info-overlay.directive';

describe('InfoOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new InfoOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
